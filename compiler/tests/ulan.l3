/*int fib_memo(int[] A, int n)
{
  if(n < 2)
    return n;
  if(A[n] != 0)
    return A[n];
  int res = fib_memo(A, n-1) + fib_memo(A, n-2);
  A[n] = res;
  return res;
}

int main()
{
  int n = 50000;
  int[] A = alloc_array(int, n + 1);
  return fib_memo(A, n);
}*/

//test memerror

struct bar {
  int hi;
};


int main () {
  int x = 5;
  struct bar* m;

  while( x < 10) { x++; }
  if(x >= 10)
    m = NULL;
  else
    m = alloc(struct bar);

  m->hi = 0 / 0;

  return 1;

}