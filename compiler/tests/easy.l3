//test error
/* recursively defined struct */

struct tree;

struct list {
  struct tree head;
  struct list * tail;
};

struct tree {
  int data;
  struct list subtrees;
};

int main ()
{
  int[] a = alloc_array(int, 2);
  a[1] = 1;
  return a[0] + a[1];
}
