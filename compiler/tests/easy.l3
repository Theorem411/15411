int f(int a, int b, int c) {
 return a + b + c;
}

int main() {
  int y = 1;
  int x = 0;
  while (y < 10) {
    if (y > 0) {
      y += f(y, y, y);
    } else {
      y += 1;
    }
  }
  x += y;
  return x;
}
